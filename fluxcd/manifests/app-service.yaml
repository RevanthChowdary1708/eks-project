apiVersion: v1
kind: Service
metadata:
  namespace: fluxcd-demo
  name: sample-app
  labels:
    app: sample-app
    project: fluxcd-demo
spec:
  type: NodePort
  selector:
    app: sample-app
    project: fluxcd-demo
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80